$("#book_comment").html("<%= j(render 'book_comments/index', book_comments: @book.book_comments) %>");
$("#book_form").html("<%= j(render 'book_comments/form', book: @book, book_comment: @comment) %>");

$("textarea").val('');

